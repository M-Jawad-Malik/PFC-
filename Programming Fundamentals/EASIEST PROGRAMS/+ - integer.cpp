#include<iostream>
using namespace std;
int main()
{
	int number;
	cout<<"enter nummber =";
	cin>>number;
	if(number>0)
    {
    	cout<<"number "<<number<<" is positive integer"; 
	 } 
	  else if(number<0)
	 {
	 	cout<<"number "<<number<<"is negative integer";
	 }
	 return 0;
}
 
